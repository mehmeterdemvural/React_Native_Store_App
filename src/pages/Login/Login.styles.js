import {StyleSheet, Dimensions} from 'react-native';

const styles = StyleSheet.create({
  container: {flex:1},
  logoContainer: {height: Dimensions.get('window').height /4},
  bodyContainer: {flex:1},
});

export {styles};
